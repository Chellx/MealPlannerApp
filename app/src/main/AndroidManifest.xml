<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mealplannerapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CurrentMealPlan"></activity>
        <activity android:name=".ViewCurrentShopList" />
        <activity android:name=".ShoppingHomePage" />
        <activity
            android:name=".ShoppingList"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".MealPlanPage" />
        <activity android:name=".UserProfile" />
        <activity android:name=".UserHomePage" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreen" />
        <activity android:name=".LoginAndRegister" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden" />
    </application>

</manifest>